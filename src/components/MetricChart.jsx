export default function MetricChart({ label, data, unit, accent="teal" }) { const max=Math.max(...data),min=Math.min(...data), points=data.map((v,i)=>`${i*(280/(data.length-1))},${90-((v-min)/(max-min||1))*60}`).join(" "); return <article className={`chart ${accent}`}><div><span>{label}</span><strong>{data.at(-1)}<small>{unit}</small></strong><em>24H</em></div><svg viewBox="0 0 280 100" preserveAspectRatio="none" aria-label={`${label} trend`}><polyline points={points}/><line x1="0" y1="90" x2="280" y2="90"/></svg></article> }
